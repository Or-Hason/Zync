"""Implementation halves of the job persistence layer.

Import from :mod:`app.services.job_repository` instead — it is the public facade
that re-exports everything here. This package exists only to keep each module
under the file-size limit.
"""
