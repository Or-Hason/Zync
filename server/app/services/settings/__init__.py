"""Settings persistence, split by domain (blacklist, notifications, scan, template).

Import from :mod:`app.services.settings_store` instead — it is the public facade
that re-exports the composed store.
"""
