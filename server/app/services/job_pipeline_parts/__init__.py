"""Implementation halves of the job scoring pipeline.

Import from :mod:`app.services.job_pipeline` instead — it is the public facade
that re-exports everything here. This package exists only to keep each module
under the file-size limit.
"""
